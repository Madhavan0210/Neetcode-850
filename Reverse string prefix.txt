class Solution {
public:
    string reversePrefix(string s, int k) {
        int n=s.length();
        int l=0,r=k-1;
        while(l<r)
        {
            char c=s[l];
            s[l]=s[r];
            s[r]=c;
            l++;
            r--;
        }
        return s;
    }
};
